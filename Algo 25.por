programa {
	funcao inicio() {
		inteiro a,b,x
		escreva("determine a primeira variavel: ")
		leia(a)
		escreva("determine a segunda variavel: ")
		leia(b)
		x = a
	    a = b
	    b = x
	    escreva("a primeira variavel é ", a, " e a segunda é ", b)
	}   
}
